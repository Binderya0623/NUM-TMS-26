server:
  port: 8082

spring:
  application:
    name: authorization-service
  main:
    allow-circular-references: true
  datasource:
    url: ${AUTHORIZATION_DB_URL:jdbc:postgresql://localhost:5432/authorization_service_db}
    username: ${AUTHORIZATION_DB_USERNAME:postgres}
    password: ${AUTHORIZATION_DB_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  cloud:
    compatibility-verifier:
      enabled: false
eureka:
  client:
    enabled: false
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/ 
